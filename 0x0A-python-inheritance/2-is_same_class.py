#!/usr/bin/python3
"""
a module that returns true
if an object is an instance of a class
"""


def is_same_class(obj, a_class):
    """
    a function that returns true
    if an object is an instance of a class
    """
    return type(obj) == a_class
